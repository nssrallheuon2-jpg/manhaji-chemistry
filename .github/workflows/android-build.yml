name: Build Expo Android APK

on:
  push:
    branches:
      - main   # يبني كل مرة تدفع كود إلى الفرع الرئيسي

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4   # الإصدار الجديد

      - name: Set up Node.js
        uses: actions/setup-node@v4 # الإصدار الجديد
        with:
          node-version: '24'        # Node.js 24 (الأحدث والمدعوم)

      - name: Install dependencies
        run: npm install

      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: Configure Expo Token
        run: |
          export EXPO_TOKEN=${{ secrets.EXPO_TOKEN }}
          eas whoami

      - name: Build APK
        run: eas build -p android --profile production --non-interactive

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: ./build/**/*.apk
